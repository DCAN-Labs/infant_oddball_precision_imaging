This folder contains the code to create the violin plots that demonstrate task stability between split halves.

The sbatch submission script is the starting point for the analysis. Analyses builds on outputs from the XCP-D pipeline. 

The conda environment 'cabinet' that is loaded contains the dependencies for the task analysis script in python:

nibabel, nilearn, numpy, pandas
